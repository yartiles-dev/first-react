import styled from 'styled-components'

export const styles = {
  DivCardList: styled.div`
   width: 85vw;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr 1fr 1fr;
  grid-gap: 20px;   
`
}